[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = src ../shared .
markers =
    contract: Schemathesis + Hypothesis API contract tests (may be slower; see tests/contract/schemathesis_config.py)
