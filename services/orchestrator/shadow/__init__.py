"""In-process Shadow helpers (hooks, NATS tools, prompts) owned by the orchestrator.

Not the ``shadow_agent`` HTTP service. Compat install surface: ``services/shadow``
(removal gate documented in that pyproject.toml).
"""
