# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: internal_comms.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass
from typing import (
    TYPE_CHECKING,
    Dict,
    List,
    Optional,
)

import betterproto
import grpclib
from betterproto.grpc.grpclib_server import ServiceBase

if TYPE_CHECKING:
    import grpclib.server
    from betterproto.grpc.grpclib_client import MetadataLike
    from grpclib.metadata import Deadline


@dataclass(eq=False, repr=False)
class SignalResolutionRequest(betterproto.Message):
    trace_id: str = betterproto.string_field(1)
    """
    Correlates downstream logs with decision/evaluation traces (W3C trace id hex, etc.).
    """

    tenant_id: str = betterproto.string_field(2)
    """Tenant / realm identifier for isolation and routing."""

    signal_names: List[str] = betterproto.string_field(3)
    """
    Logical signal identifiers (e.g. feature store keys) to resolve in one round-trip.
    """

    evaluation_context_json: bytes = betterproto.bytes_field(4)
    """
    Opaque JSON blob: evaluation payload slice the provider may need (bounded by policy at callers).
    """


@dataclass(eq=False, repr=False)
class ResolvedSignal(betterproto.Message):
    name: str = betterproto.string_field(1)
    ok: bool = betterproto.bool_field(2)
    payload_json: bytes = betterproto.bytes_field(3)
    """
    JSON-encoded value when ok; empty when not ok unless partial payloads are contractually allowed.
    """

    error_code: str = betterproto.string_field(4)
    """
    Stable machine-readable reason when ok is false (e.g. SIGNAL_NOT_FOUND, UPSTREAM_TIMEOUT).
    """

    error_detail: str = betterproto.string_field(5)


@dataclass(eq=False, repr=False)
class SignalResolutionResponse(betterproto.Message):
    signals: List["ResolvedSignal"] = betterproto.message_field(1)
    provider_latency_ms: int = betterproto.uint32_field(2)
    """
    Optional hint: provider-side processing time excluding network to caller.
    """


@dataclass(eq=False, repr=False)
class PingRequest(betterproto.Message):
    caller_service: str = betterproto.string_field(1)
    """Calling service name for debugging (e.g. decision-api)."""


@dataclass(eq=False, repr=False)
class PongResponse(betterproto.Message):
    callee_service: str = betterproto.string_field(1)
    build_version: str = betterproto.string_field(2)


class SignalServiceStub(betterproto.ServiceStub):
    async def resolve_signals(
        self,
        signal_resolution_request: "SignalResolutionRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "SignalResolutionResponse":
        return await self._unary_unary(
            "/tarka.internal.v1.SignalService/ResolveSignals",
            signal_resolution_request,
            SignalResolutionResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def ping(
        self,
        ping_request: "PingRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "PongResponse":
        return await self._unary_unary(
            "/tarka.internal.v1.SignalService/Ping",
            ping_request,
            PongResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )


class SignalServiceBase(ServiceBase):

    async def resolve_signals(
        self, signal_resolution_request: "SignalResolutionRequest"
    ) -> "SignalResolutionResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def ping(self, ping_request: "PingRequest") -> "PongResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def __rpc_resolve_signals(
        self,
        stream: "grpclib.server.Stream[SignalResolutionRequest, SignalResolutionResponse]",
    ) -> None:
        request = await stream.recv_message()
        response = await self.resolve_signals(request)
        await stream.send_message(response)

    async def __rpc_ping(
        self, stream: "grpclib.server.Stream[PingRequest, PongResponse]"
    ) -> None:
        request = await stream.recv_message()
        response = await self.ping(request)
        await stream.send_message(response)

    def __mapping__(self) -> Dict[str, grpclib.const.Handler]:
        return {
            "/tarka.internal.v1.SignalService/ResolveSignals": grpclib.const.Handler(
                self.__rpc_resolve_signals,
                grpclib.const.Cardinality.UNARY_UNARY,
                SignalResolutionRequest,
                SignalResolutionResponse,
            ),
            "/tarka.internal.v1.SignalService/Ping": grpclib.const.Handler(
                self.__rpc_ping,
                grpclib.const.Cardinality.UNARY_UNARY,
                PingRequest,
                PongResponse,
            ),
        }
