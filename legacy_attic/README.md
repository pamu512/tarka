# legacy_attic

Only the load-bearing leftover from the v1.3 hoist:

| Path | Why it stays |
|------|----------------|
| `triggers/immutable_cases.sql` | shared-core gate still pins this path |

Root cargo-fuzz is `fuzz/`. Compose front doors stay at repo root.
