# Release v1.1.0 (Planned)

Planned release date: `2026-04-30`

## Theme

Consortium intelligence v2, inference contract v2, analyst-facing explainability, plus **CI/security hygiene and a documented five-minute eval path**—one coordinated ship.

## Planned Highlights

### Consortium (quality and policy)

- Weighted consortium scoring with tenant trust.
- False-positive suppression loop for consortium outcomes.
- Tenant-level consortium participation policies.
- Consortium quality dashboard metrics.

### Inference and decision contract (Epics A, B, E—core)

- Versioned **`inference_context` (schema v2)** with **`confidence_tier`** (low / medium / high).
- **`driver_reasons`**: analyst-oriented top drivers (tamper, replay, network, geo, ML, rules, colocation, travel proxy).
- **Replay / tamper context** reflected in derived risks and drivers (ingress replay signals, rule-based replay hints).
- **Co-location and impossible-travel proxies** (`colocation_risk`, `impossible_travel_risk`) from device and velocity/geo heuristics.
- **Velocity in inference**: `velocity_events_5m`, `velocity_events_1h`, `velocity_events_24h` (aggregates include **5m** window).
- **`recommended_action`** on evaluate and audit responses (e.g. `block`, `manual_review`, `step_up_mfa`, `step_up_attestation`) for client step-up hints.

### APIs, contracts, and SDKs

- OpenAPI **`InferenceContext`** and **`EvaluateResponse`** updates for v2 fields and **`recommended_action`**.
- **Python** and **TypeScript** fraud SDK types aligned with the evaluate response.

### Frontend and demos

- Case detail **explainability panel**: tier, schema version, velocity row, travel/colocation when elevated, top drivers, recommended action.
- Mock evaluate/audit payloads updated for v2 (demo and offline dev).

### Simulation and guardrails

- **`/v1/simulation/run`** response includes **`experiment_guardrails`** (sample size, determinism, KPI caveats).

### Tests and validation

- Unit coverage for **`inference_build`** (tiering, velocity, travel/colocation, **`derive_recommended_action`**).
- **`pytest`** for **`/v1/replay`** paired **`trace_ids`** mode (order, **`missing_trace_ids`**, empty-window 404).
- **Golden `inference_context` v2 keys** in **`contracts/golden/`** enforced by decision-api tests (calibration / parity shell).
- **OpenAPI contract hygiene:** checked-in YAML must **parse** and declare **`openapi` / `info` / `paths`** (pytest); **FastAPI** generated schema smoke for **`/v1/decisions/evaluate`** and **`inference_context`** (strict OAS 3.1 on static files is incremental).
- **Investigation agent security tests:** tenant-scoped tool calls (ignore LLM-supplied `tenant_id`); injection / redaction coverage.
- **Live Neo4j smoke** in CI (**`test-graph-neo4j-live`**) — Bolt `RETURN 1` against **`neo4j:5-community`**.

### CI/CD, security hygiene, and first-run polish (ships with v1.1.0 train)

- **GitHub Actions CI** (`main` / `master`): Ruff; **decision-api** tests with coverage gate (**≥45%**, path to 60%+); **case-api**, **Python SDK**; **graph-service** (Neo4j-mocked algorithms); **integration-ingress** (full suite + OSINT parallel tests, `PYTHONPATH` includes **`services/shared`**); **investigation-agent**; **graphql-gateway**, **event-ingest**, **analytics-sink**, **feature-service**, **ml-scoring** (pytest + coverage XML where configured); **frontend** `npm ci` + **`npm run build`**; **`packages/fraud-sdk-typescript`** **`npm ci` + `npm run build`**; **Docker builds** gated on all test and Node build jobs.
- **PostgreSQL schema:** **Alembic** migrations under **`services/decision-api/alembic/`** and **`services/case-api/alembic/`** — **`alembic upgrade head`** on service startup when using PostgreSQL; SQLite (CI/unit) still uses **`create_all`**.
- **GraphQL gateway** exposes the same **`/metrics`** and structured request logging as other FastAPI services (**`services/shared/observability.py`**).
- **Security scanning workflow**: **Trivy** filesystem + **decision-api** image → **SARIF** upload (where code scanning is enabled); weekly schedule.
- **Dependabot**: grouped updates for **GitHub Actions**, **pip** (core services), **npm** (frontend).
- **Docs:** **`SECURITY.md`** (responsible disclosure), **`LICENSE-DEPENDENCIES.md`** (Neo4j AGPL / lite and alternates), **`CODE_OF_CONDUCT.md`**, **`docs/docs/guides/security-scanning.md`**, **`docs/docs/guides/sandbox-five-minute.md`** (copy-paste evaluate + OSINT + UI path).
- **Onboarding:** **`.devcontainer/devcontainer.json`** (Codespaces / Docker-outside-Docker); **README** badges (CI, security scan, Codespaces); **walkthrough video** placeholder for maintainer Loom link.
- **`deploy/docker-compose.lite.yml`**: adds **integration-ingress** (**8003**) so lite stack matches the five-minute OSINT demo without full Neo4j.

**Gap-closure schedule (what ships 4/30 vs 5/30):** [`docs/docs/guides/release-gap-closure-schedule.md`](../guides/release-gap-closure-schedule.md).

## v1.1.0 acceptance checklist (evidence before tag)

Use this section as the **definition of done** for `v1.1.0`. For each row, fill **Evidence** (PR link, path under `contracts/` or `services/`, CI run URL, screenshot, or “N/A / deferred”) and **Owner** before tagging.

### Theme & inference (Epics A–E)

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| `inference_context` schema v2 with `confidence_tier` | | | |
| `driver_reasons` populated in inference path | | | |
| Replay/tamper reflected in risks & drivers | | | |
| `colocation_risk`, `impossible_travel_risk` in inference | | | |
| Velocity: `velocity_events_5m` / `_1h` / `_24h` | | | |
| `recommended_action` on evaluate + audit | | | |

### APIs, contracts, SDKs

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| OpenAPI `InferenceContext` + `EvaluateResponse` ([`contracts/openapi/decision-api.yaml`](../../contracts/openapi/decision-api.yaml)) | | | |
| Python SDK types aligned with evaluate response | | | |
| TypeScript SDK `InferenceContext` aligned with same schema ([`packages/fraud-sdk-typescript`](../../../packages/fraud-sdk-typescript)) | | | |

### Frontend and demos

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| Case detail **Decision explainability**: tier, schema version, velocity row, travel/colocation when elevated, drivers, `recommended_action` ([`frontend/src/pages/CaseDetail.tsx`](../../../frontend/src/pages/CaseDetail.tsx)) | | | |
| Partial audit JSON hardened via `normalizeInferenceContext` ([`frontend/src/api/inferenceContext.ts`](../../../frontend/src/api/inferenceContext.ts)) | | | |
| Mock evaluate/audit v2 payloads ([`frontend/src/api/mockData.ts`](../../../frontend/src/api/mockData.ts)) | | | |
| Five-minute doc includes Case Detail path ([`sandbox-five-minute.md`](../guides/sandbox-five-minute.md)) | | | |

### Simulation

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| `/v1/simulation/run` returns `experiment_guardrails` | | | |

### Tests & validation (backend)

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| `inference_build` unit coverage (tiering, velocity, travel/colocation, `derive_recommended_action`) | | | |
| Pytest `/v1/replay` paired `trace_ids` | | | |
| Golden `inference_context` v2 keys in `contracts/golden/` | | | |
| OpenAPI YAML parse / structure checks + schema smoke | | | |
| Investigation agent security tests (tenant scope, injection/redaction) | | | |
| Live Neo4j smoke job (`test-graph-neo4j-live`) | | | |

### CI/CD, security, docs, compose

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| GitHub Actions CI green: Ruff, service test matrix, **`build-frontend`** / **`build-sdk-typescript`** (Node 20, `NODE_OPTIONS=--max-old-space-size=4096` on Node jobs) | | | |
| Alembic on Postgres for decision-api + case-api | | | |
| GraphQL gateway `/metrics` + structured logging parity | | | |
| Trivy + Dependabot configured | | | |
| `SECURITY.md`, `LICENSE-DEPENDENCIES.md`, `CODE_OF_CONDUCT.md`, security-scanning guide, **sandbox-five-minute** | | | |
| `.devcontainer`, README badges | | | |
| `deploy/docker-compose.lite.yml` includes integration-ingress **8003** | | | |

### Pre-tag validation (from § Planned Validation)

| Criterion | Evidence | Owner | Date |
|-----------|----------|-------|------|
| Consortium / feedback-loop tests (as applicable) | | | |
| Contract tests for `inference_context` v2 + `recommended_action` | | | |
| `pytest` decision-api + frontend `npm run build` + TS SDK `npm run build` on release candidate commit | | | |
| Full CI workflow green on default branch | | | |
| Trivy workflow completes | | | |
| Lite compose smoke: **8000** evaluate, **8003** OSINT, **3000** UI | | | |
| **`investigation-agent`** `pytest` (Investigation Copilot tool loop, security tests) green | | | |
| **Security scan** workflow ([`.github/workflows/security-scan.yml`](../../../.github/workflows/security-scan.yml)) green / completed on RC commit (Trivy FS + decision-api image jobs) | | | |

## Release candidate finalization & May handoff policy

**Goal for `v1.1.0`:** ship with **no failing automated release gates** and **no known P0 blockers** on the **demo path** (lite compose + five-minute doc) and the **full stack** profile used in CI (`docker-build` / `stack-smoke` matrix). *“Zero bugs”* is not mathematically provable; **operational definition** = all required CI jobs pass, security workflows complete without blocking failures, and open **P0** issues affecting demo or tagged stack are **closed or explicitly waived** in the checklist above.

**Minimum bar before tagging `v1.1.0`:**

1. **[`ci.yml`](../../../.github/workflows/ci.yml)** — full matrix green on the release candidate commit: **lint (Ruff)**; **frontend** `npm ci` + `npm run build`; **`investigation-agent`** tests; decision-api (coverage gate), case-api, graph-service, Neo4j live job, integration-ingress, SDK Python, GraphQL, event-ingest, analytics-sink, feature-service, ml-scoring; **TypeScript SDK** build; **docker-build**; **stack-smoke** as configured.
2. **[`security-scan.yml`](../../../.github/workflows/security-scan.yml)** — completed run on the same commit (Trivy filesystem + decision-api image; SARIF upload may be org-dependent).
3. **Manual smoke:** [`sandbox-five-minute.md`](../guides/sandbox-five-minute.md) (evaluate + OSINT + **Case detail explainability** + copilot/mock path as applicable).

**After `v1.1.0` is tagged (finalized build):** routine product and engineering changes that **miss** the 4/30 tag **do not** retroactively alter `v1.1.0`. They ship on the **May 2026 Friday trains** documented in [`release-calendar-2026-05.md`](../guides/release-calendar-2026-05.md) and roll into **`v1.2.0` (`2026-05-30`)** per [`RELEASE_SCHEDULE.md`](../../../RELEASE_SCHEDULE.md), unless explicitly cherry-picked as a **patch** (document any exception in release notes).

**Local subset (frontend + copilot + TS SDK + Ruff on `services/investigation-agent`):** from repo root, `bash scripts/ci/verify_release_candidate.sh` — does **not** replace **`ruff check .`** on the whole repo, decision-api coverage, Docker build, stack-smoke, or other service pytests (those stay on **GitHub Actions `ci.yml`**).

## Maintainer runbook (push → PR → CI → RC script → evidence → tag)

1. **Commit** on a branch (e.g. `release/v1.1.0-rc` or `chore/v1.1.0-prep`) with all intended changes; do not commit local `.coverage` or `services/**/*.db` (see root `.gitignore`).
2. **Push** the branch to `origin` and **open a pull request** to `main` / `master` (GitHub UI or `gh pr create`).
3. Wait until **[`ci.yml`](../../../.github/workflows/ci.yml)** and **[`security-scan.yml`](../../../.github/workflows/security-scan.yml)** complete **successfully** on the PR (or merge commit).
4. On **Linux or macOS**, from repo root: `bash scripts/ci/verify_release_candidate.sh`  
   On **Windows**, fix `npm ci` issues (file locks / AV) or run the same Node steps in WSL; the script’s **Ruff** slice targets `services/investigation-agent` only—full `ruff check .` is still the **lint** job in CI.
5. **Fill evidence** in the acceptance checklist tables above (links to Actions runs, PR, screenshots).
6. **Merge** the PR when green. On the **exact merge commit** (or chosen RC SHA), tag:  
   `git tag -a v1.1.0 -m "v1.1.0 — see docs/docs/releases/v1.1.0-2026-04-30.md"`  
   `git push origin v1.1.0`  
   Do **not** tag until steps 3–5 are satisfied.

## Single release sign-off (v1.1.0)

One coordinated **“go”** for `2026-04-30` after the tables above are complete (or explicitly waived with evidence).

| Role | Name | Signature / date | Notes |
|------|------|------------------|-------|
| Engineering lead | | | |
| Product / program | | | |
| Security / compliance (if required) | | | |

## Planned Breaking Changes

- None expected. Clients should tolerate new optional/v2 fields; older clients ignoring unknown JSON keys remain compatible.

## Planned Validation

- Integration tests for weighted consortium scoring.
- End-to-end feedback loop tests from case outcome to decision score impact.
- API and UI tests for consortium policy controls.
- Contract tests for evaluate/audit **`inference_context`** v2 and **`recommended_action`**.
- **`pytest`** (decision-api) and frontend **`npm run build`** green before tag; **TypeScript SDK** **`npm run build`** green.
- **CI workflow green** on default branch: lint, all Python service test jobs (including graphql + streaming path services), Node builds, Docker build matrix.
- **Trivy** security workflow completes (SARIF upload may depend on org plan); **Dependabot** enabled for the repository.
- **Lite compose** smoke: `docker compose -f deploy/docker-compose.lite.yml up -d --build` → **8000** evaluate, **8003** OSINT health, **3000** frontend reachable.
