# v1.2.5 execution backlog — resiliency, ETL stability, no-code rules

**Branch train:** `ide/v1.2.5-7320` (merge epics via focused PRs; link each PR to this doc section).

**Ordering (fastest ROI first):** ETL contract + quality gates → no-code rule workflow → degradation/SLO burn → rule telemetry → chaos + replay certification.

---

## Epic R1 — Platform hardening & SLOs

| ID | Deliverable | Surfaces | CI / ops |
|----|-------------|----------|----------|
| R1.1 | **SLO definitions** (availability, p95/p99 latency, queue lag) per service: decision-api, case-api, integration-ingress, feature-service, ml-scoring | `docs/docs/guides/service-slos-v1.md` (new), link from deployment | Optional: Prometheus recording rules template in `deploy/observability/` |
| R1.2 | **`/v1/slo` parity** — extend each service with `targets` + `current` counters (where missing) | FastAPI handlers | Contract tests |
| R1.3 | **Burn-rate alerts** — fast window (5m) + slow window (1h) doc + example Alertmanager / Grafana | `deploy/observability/` README | Nightly validate YAML |
| R1.4 | **Error rate by route** — middleware label `path` + `tenant_id` where safe | shared `observability` | Dashboard JSON optional |

---

## Epic R2 — Failure isolation & degradation

| ID | Deliverable | API / code |
|----|-------------|------------|
| R2.1 | **Circuit breaker** wrapper for httpx outbound (graph, ML, feature, OPA, currency) | `services/shared/` or per-service `circuit.py` |
| R2.2 | **Tags on degrade** — `enrichment:unavailable`, `ml:unavailable`, `graph:unavailable`; inference driver + tier bump | decision-api `inference_build` / evaluate path |
| R2.3 | **Per-tenant flags** — Redis or DB keyed `tenant:{id}:flags` (kill-switch enrichers) | decision-api + small admin GET |
| R2.4 | **ML down → rules-only** — already partial; unify reason string + audit field `fallback_reason` | evaluate + OpenAPI |

---

## Epic R3 — Idempotency & exactly-once-ish

| ID | Deliverable | Status |
|----|-------------|--------|
| R3.1 | **Require `Idempotency-Key`** on high-volume ingest paths (config flag) | event-ingest `INGEST_REQUIRE_IDEMPOTENCY_KEY` |
| R3.2 | **Decision evaluate** — document + optional require header (gateway) | decision-api + docs |
| R3.3 | **ETL dedupe** — bronze table unique key `(source, event_id)` + quarantine table | future: analytics-sink / warehouse |
| R3.4 | **Consumer idempotency** — decision worker uses trace key from payload | event-ingest consumer |

---

## Epic R4 — Chaos & recovery

| ID | Deliverable |
|----|---------------|
| R4.1 | `scripts/chaos/README.md` + compose override profiles (stop redis, delay postgres) |
| R4.2 | GitHub workflow `chaos-smoke` (optional manual dispatch) running health + evaluate after fault inject |
| R4.3 | Runbook template in `docs/docs/guides/runbook-chaos-template.md` |

---

## Epic E1 — Contract-first ingestion (first slice **shipped**)

| ID | Deliverable | Location |
|----|-------------|----------|
| E1.1 | **Ingest envelope v1** — optional wrapper `{ "schema_version": "1", "event": { ... } }` + `INGEST_ENVELOPE_MODE` optional\|required | `services/event-ingest` |
| E1.2 | **Reject/quarantine** — `422` + `reason_codes[]` for unknown `event_type`, empty ids, wrong schema | same |
| E1.3 | **Metrics** — `ingest_contract_reject_total`, envelope version counter | Prometheus text |
| E1.4 | **Compatibility policy** doc — backward compatible until `schema_version` bump | this file § below |

**Compatibility:** `schema_version` omitted = legacy flat body (unchanged). `schema_version: "1"` requires `event` object. Breaking changes only bump schema major.

---

## Epic E2 — Bronze / Silver / Gold

| ID | Deliverable |
|----|-------------|
| E2.1 | Doc: physical mapping (NATS raw → worker → Postgres audit = bronze-ish; gold = aggregates) — **[etl-bronze-silver-gold.md](./etl-bronze-silver-gold.md)** |
| E2.2 | **`scripts/etl/check_silver_features.py`** — JSONL null/enum/amount checks + max violation rate |
| E2.3 | **`INGEST_DLQ_*`** + **`scripts/etl/replay_dlq.py`** — DLQ publish on evaluate 4xx (opt-in); replay CLI |

---

## Epic E3 — Late arrival & watermark (**shipped**)

| ID | Deliverable | Location |
|----|-------------|----------|
| E3.1 | Doc: event_time vs ingest_time, watermark for counters | [late-arrival-watermarks.md](./late-arrival-watermarks.md) |
| E3.2 | feature-service / counter replay alignment | `event_time_unix_for_evaluate` in **`services/shared/event_time.py`**; evaluate uses it for **`record_event`**; **`export_audit_to_jsonl.py`** + **`replay_aggregates.py`** use the same rules; OpenAPI **`metadata`** description |

---

## Epic E4 — ETL observability “product”

| ID | Deliverable |
|----|-------------|
| E4.1 | Frontend route `/ops/pipelines` (stub) listing ingest reject rate from new metrics endpoint |
| E4.2 | event-ingest **`GET /v1/ingest/stats`** — in-process `reason_codes` counts since boot (Redis optional later) |

---

## Epic N1 — No-code rule builder

| ID | Deliverable | UI |
|----|-------------|-----|
| N1.1 | **Simulation API** — POST batch of features → shadow decision delta histogram | decision-api or new `rules-sim` |
| N1.2 | **Rule composer** — extend `static/rule-builder.html` or React wizard | frontend |
| N1.3 | **Impact summary** — top tags / entities from sim (sampled) | API response |

---

## Epic N2 — Maker-checker & staged rollout

| ID | Deliverable |
|----|-------------|
| N2.1 | DB table `rule_pack_revisions` (draft, pending_review, approved, canary_pct) |
| N2.2 | UI: approval queue + canary % (reuse governance patterns from `rule_api.py`) |
| N2.3 | Auto-rollback — if canary FP rate > threshold (define in YAML) |

---

## Epic N3 — Rule lint / guardrails

| ID | Deliverable |
|----|-------------|
| N3.1 | Extend `scripts/policy/validate_rule_packs.py` — max conditions, max score_delta, regex depth |
| N3.2 | **Conflict detection** — shadow overlap report (same feature, opposite actions) |

---

## Epic N4 — Rule telemetry

| ID | Deliverable |
|----|-------------|
| N4.1 | Prometheus: `rule_hit_total{rule_id=...}` from evaluate path |
| N4.2 | Weekly aggregate export (stub → #53) — **`scripts/analytics/export_weekly_scorecard_json.py`** |

---

## Epic X — Cross-cutting metadata

| ID | Deliverable |
|----|-------------|
| X.1 | `counter_version`, `rule_pack_file`, `ml_model` on every audit row (partially exists — unify) |
| X.2 | `etl_batch_id` optional on ingest envelope v2 (future) |
| X.3 | **Replay parity gate** in CI — threshold in `scripts/replay/` README |
| X.4 | **Tenant reliability profile** — `strict|balanced|permissive` env doc + decision-api read |

---

## Release gates (v1.2.5)

Block tag / branch protection checks when:

1. SLO burn synthetic test fails (once R1.3 exists).
2. ETL contract reject rate integration test fails (once E4.2 + CI wired).
3. Replay parity below threshold (existing scripts + new gate).

---

## Implementation status (update when merging)

| Slice | PR | Merged |
|-------|-----|--------|
| E1 + E2 (partial) + E4.2 (stub) | [PR #95](https://github.com/pamu512/tarka/pull/95) (`ide/v1.2.5-etl-contract-e1-7320`) | _pending merge_ |
| E3 (late arrival / watermark doc + replay alignment) | `ide/v1.2.5-etl-e3-watermark-7320` | _this branch_ |
