# Global baseline AI governance build (ISO 42001–style themes; local law still applies)
# Copy into deploy/.env or inject via your orchestrator.

AI_GOVERNANCE_PROFILE=global
VITE_AI_GOVERNANCE_PROFILE=global

# BATCH_TTL_SECONDS=7200
