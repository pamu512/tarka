# US AI governance build — use with: docker compose -f infra/deploy/docker-compose.yml -f infra/deploy/profiles/ai-governance/docker-compose.override-us.yml ...
services:
  investigation-agent:
    environment:
      AI_GOVERNANCE_PROFILE: us
  frontend:
    build:
      args:
        VITE_AI_GOVERNANCE_PROFILE: us
