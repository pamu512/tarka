{{- $inlineCreds := and .Values.coreApi.databaseUrl .Values.coreApi.redisUrl .Values.coreApi.natsUrl }}
{{- $useSecret := or .Values.credentials.create .Values.credentials.existingSecretName }}
{{- if and .Values.integrationIngress.enabled (not $inlineCreds) (not $useSecret) }}
{{- fail "integration-ingress: requires Secret with DATABASE_URL or coreApi inline credentials." }}
{{- end }}
{{- if .Values.integrationIngress.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tarka.fullname" . }}-integration-ingress
  labels:
    {{- include "tarka.labels" . | nindent 4 }}
    tarka.io/component: integration-ingress
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "tarka.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    tarka.io/component: integration-ingress
  ports:
    - name: http
      port: {{ .Values.integrationIngress.service.port }}
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tarka.fullname" . }}-integration-ingress
  labels:
    {{- include "tarka.labels" . | nindent 4 }}
    tarka.io/component: integration-ingress
spec:
  replicas: {{ .Values.integrationIngress.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "tarka.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      tarka.io/component: integration-ingress
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "tarka.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        tarka.io/component: integration-ingress
        {{- with .Values.global.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "tarka.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: integration-ingress
          image: {{ include "tarka.containerImage" (dict "root" . "repository" .Values.integrationIngress.image.repository "tag" .Values.integrationIngress.image.tag) }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.integrationIngress.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "tarka.platformConfigMap" . }}
          {{- if and $useSecret (not $inlineCreds) }}
            - secretRef:
                name: {{ include "tarka.credentialsSecret" . }}
          {{- end }}
          env:
            {{- if $inlineCreds }}
            - name: DATABASE_URL
              value: {{ .Values.coreApi.databaseUrl | quote }}
            {{- end }}
            - name: TENANT_BINDING_REQUIRED
              value: {{ .Values.integrationIngress.env.tenantBindingRequired | quote }}
          resources:
            {{- toYaml .Values.integrationIngress.resources | nindent 12 }}
          startupProbe:
            {{- toYaml .Values.integrationIngress.startupProbe | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.integrationIngress.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.integrationIngress.readinessProbe | nindent 12 }}
{{- end }}
