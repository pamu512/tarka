__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
.venv_pb2/
.venv-internal-comms/
venv/

# Local-only Development CA + keys for Envoy mTLS (see infra/deploy/observability/envoy/scripts/gen-dev-mtls-certs.sh).
infra/deploy/observability/envoy/certs/dev/
.env
*.onnx
*.pkl
*.pickle

# Standalone KMS crate (outside workspace); lockfile is generated locally only.
crates/tarka-kms/Cargo.lock

node_modules/
dist/
.tsbuildinfo
*.tsbuildinfo

.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# Local AI-assisted editor metadata — keep the repo vendor-neutral; do not commit.
.cursor/

# Local AI stack runtime configuration
deploy/local-ai/.env.local

# Generated Helm values (scripts/deploy/generate_cloud_values.py, CI smoke)
deploy/generated/

# Python coverage (local pytest)
.coverage
**/.coverage
**/coverage.xml

# Local SQLite files under services (dev/demo; not release artifacts)
services/**/*.db

# ml-scoring OnlineAnomalyDetector persisted state (local dev / tests)
**/adaptive_state.json

# Investigation agent local data dir (feedback, RAG, review DB paths in dev)
services/investigation-agent/var/
/var/okf-staging/

# decision-api local runtime logs (evaluate JSONL, etc.)
services/decision-api/data/

# Orchestrator local AI feedback export (``POST /v1/ai/feedback`` JSONL sink)
**/ai_rejection_feedback.jsonl

# L3 host-action sink (append-only runtime; ledger JSON stays tracked)
docs/compliance/host_action_log.jsonl

# Rust build trees (never commit; TruffleHog/CodeQL noise if tracked)
**/target/

# Local gate / benchmark CSV dumps (not source)
artifacts/

# Next.js build output
tarka_v2_ui/.next/

# Local runtime / e2e scratch (never commit)
.tarka-micro-e2e/
/data/
test-results/
/rules/enforcement_delivery.jsonl
# docs/site is generated by mkdocs — do not commit
docs/site/

# Brainstorm visual companion sessions (local mockups)
.superpowers/
