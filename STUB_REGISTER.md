# Stub Register (Phase 0 inventory)

This register was produced by executing **Phase 0** of [docs/TIER_1_HONESTY_PROGRAM.md](docs/TIER_1_HONESTY_PROGRAM.md): keyword scans across `services/`, `frontend/`, and `packages/`, then manual classification. **No code was changed** as part of this inventory.

## Methodology

- **Keywords searched:** `stub`, `_stub`, `stub_`, `_STORE`, `placeholder` (code/comments, not SQL `{tenant_id:String}` bind tokens alone), module docstrings mentioning `integration stubs`, and related patterns (`echo_stub`, `backtest_run_stub`, `rego_stub`, `metric_stub`).
- **Scopes:** `services/`**, `frontend/`**, `packages/**` (Rust under `services/**` had no `stub`/`TODO`/`FIXME` hits in this pass).
- **Excluded by policy (non-goals for runtime):** `**/tests/`**, `**/test_*.py`, `unittest.mock` / `vi.mock` usage, and comments that only mention “mock” for test harnesses.
- **Disposition values:** **Ship** = replace with durable + real execution; **Delete** = remove HTTP/UI surface until honest; **Degrade** = keep only with explicit, documented contract (e.g. structured 503 / response field `source`), never silent fake metrics.

## Stub Register (runtime and operator-facing)


| ID    | File                                                                                                                                   | Symbol / route / surface                                               | User-visible behavior (today)                                                                                                                                   | Target disposition                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| SR-01 | [services/decision-api/src/decision_api/feature_store_api.py](services/decision-api/src/decision_api/feature_store_api.py)             | `POST/GET /v1/feature-store/definitions`                               | **Shipped (Wave 1):** Postgres `feature_definitions` + ClickHouse MV DDL execution (`ddl_status` applied/failed)                                                | **Done**                                                                                           |
| SR-02 | [services/decision-api/src/decision_api/feature_store_engine.py](services/decision-api/src/decision_api/feature_store_engine.py)       | `agg_value` MV column                                                  | **Shipped:** AggregatingMergeTree uses `agg_value` (no `metric_stub` alias)                                                                                     | **Done**                                                                                           |
| SR-03 | [services/decision-api/src/decision_api/analytics_dashboards.py](services/decision-api/src/decision_api/analytics_dashboards.py)       | `GET /v1/analytics/dashboards/kpis`                                    | **Shipped:** live analytics engine counts; **503** when offline (no null stub KPI payload)                                                                      | **Done**                                                                                           |
| SR-04 | [services/decision-api/src/decision_api/backtest_api.py](services/decision-api/src/decision_api/backtest_api.py)                       | `POST /v1/rules/backtest/run`                                          | **Deleted:** **410 Gone** tombstone; real path is `POST /v1/rules/backtest/jobs`                                                                                | **Done** (tombstone)                                                                               |
| SR-05 | [services/decision-api/src/decision_api/rule_compiler_api.py](services/decision-api/src/decision_api/rule_compiler_api.py)             | `POST /v1/rules/visual/compile`; `POST /v1/rules/rego/compile` (**410 Gone**, OpenAPI `deprecated`) | **Deleted:** Rego/OPA transpilation from the visual builder (no second “policy language” artifact). **Deprecated:** legacy clients hitting `POST /v1/rules/rego/compile` receive **410** with an explicit message to use native JSON compile + Rust evaluation. | **Deleted** / **Deprecated** (Track F)                                                             |
| SR-06 | [services/decision-api/src/decision_api/vendors/bootstrap.py](services/decision-api/src/decision_api/vendors/bootstrap.py)               | Fingerprint / Incognia / ip_api plugins                                      | **Shipped (Wave partner):** register real adapters when `TARKA_VENDOR_*` env set; no echo stub                                                                 | **Done**                                                                                           |
| SR-07 | [services/decision-api/src/decision_api/vendor_marketplace_api.py](services/decision-api/src/decision_api/vendor_marketplace_api.py)   | `POST /v1/vendors/probe`                                               | Probes registered adapters only (returns not-found when unset) — honest                                                                                       | **Done** (depends on SR-06)                                                                        |
| SR-08 | [services/case-api/src/case_api/sar_transport_worker.py](services/case-api/src/case_api/sar_transport_worker.py) | NATS SAR transport worker (`setup_sar_transport_worker`) | **Shipped path:** durable worker when wired; SFTP upload only if `FINCEN_BSA_SFTP_HOST` set. Legacy `schedule_ack_poll` removed. | **Done** (default: no FinCEN host = no SFTP; worker still honest) |
| SR-09 | [services/case-api/src/case_api/main.py](services/case-api/src/case_api/main.py) | SAR lifespan → `setup_sar_transport_worker` | Generate flow no longer depends on log-only ACK poll. FinCEN delivery remains env-gated. | **Done** (env-gated FinCEN) |
| SR-10 | [services/case-api/src/case_api/sar_filing_transport.py](services/case-api/src/case_api/sar_filing_transport.py)                       | `validate_pre_filing`                                                  | Best-effort substring / tag checks for required “fields”; not a full BSA XML schema validator                                                                   | Ship (validation depth)                                                                            |
| SR-11 | [services/decision-api/src/decision_api/rule_gitops_api.py](services/decision-api/src/decision_api/rule_gitops_api.py)                 | `POST /v1/rules/gitops/approve`                                        | **Shipped:** inserts durable `rule_approvals` row; **503** if persistence fails                                                                                 | **Done**                                                                                           |
| SR-12 | [services/decision-api/src/decision_api/reporting_nl.py](services/decision-api/src/decision_api/reporting_nl.py)                       | `POST /v1/reporting/nl-to-sql` when `TARKA_REPORTING_NL_LLM_URL` unset | **Honest fail-closed:** **503** `LLM_ENGINE_OFFLINE` (no template SQL fallback). Success returns `source: "llm"`. | **Done** (degraded path removed) |
| SR-13 | [frontend/src/api/client.ts](frontend/src/api/client.ts)                                                                               | `getMockResponse`, `VITE_USE_API_MOCKS`, `USE_API_MOCKS`               | Optional **mock API fallback** in dev / when forced on; can mask broken backends during demos                                                                   | Degrade (default off in prod — already); Delete (remove mocks) if policy forbids any runtime mocks |
| SR-14 | [frontend/src/pages/VisualRuleBuilder.tsx](frontend/src/pages/VisualRuleBuilder.tsx)                                                   | UI copy                                                                | Copy aligned with JSON-only compile path (no Rego export UX).                                                                                                     | **Deleted** / **Deprecated** (Track F)                                                            |
| SR-15 | [services/decision-api/src/decision_api/rule_api.py](services/decision-api/src/decision_api/rule_api.py) + `json_rules.get_rule_hit_telemetry` | `GET /v1/rules/telemetry` | Response includes `since_process_start: true` and `durability: "process_memory"` (not cluster-durable). | **Degraded** (labeled); Ship Redis/DB later |
| SR-16 | [services/integration-ingress/src/integration_ingress/sanctions.py](services/integration-ingress/src/integration_ingress/sanctions.py) | Docstring: in-memory fuzzy matching                                    | Screening behavior may not meet enterprise persistence/audit expectations if implementation matches docstring                                                   | Ship (clarify + persist)                                                                           |
| SR-17 | [services/investigation-agent/src/investigation_agent/main.py](services/investigation-agent/src/investigation_agent/main.py)           | Tabular upload handler (docstring)                                     | Describes **in-memory TTL** storage for uploaded tabular files — bounded but not durable                                                                        | Ship (disk/redis) or Degrade (strict size + documented volatility)                                 |

### Track F — architecture (native rules, no Rego transpile)

Tarka **deleted** Rego/OPA **transpilation** from the visual rule pipeline so there is a **single evaluation truth**: JSON packs and optional `when_ast` are evaluated by the native **`tarka_rule_engine`** Rust core. That removes **logic drift** (different semantics in a generated `.rego` bundle vs what production actually ran) and keeps audit and replay **brutally honest**—logs and traces reference the same engine and pack shape operators approved in GitOps. A **deprecated** **`POST /v1/rules/rego/compile`** route remains only as a **410 Gone** tombstone for old integrations; optional **OPA** at evaluate time (when configured) is a **separate** HTTP step, not a substitute for that native path.

## Packages scan (`packages/`)

- **No** `stub` / `_stub` / `_STORE` hits in `packages/tarka-web-sdk` or `packages/fraud-sdk-typescript` source (excluding field names like `mock_location_detected` on types and test `vi.mock`).

## `services/**/*.rs` scan

- **No** matches for `stub`, `TODO`, `FIXME`, or `unimplemented!` in this pass (pattern: whole-word / common markers only).

## Macroservices note

- [services/core-api/src/core_api/main.py](services/core-api/src/core_api/main.py) mounts `decision_api` and `case_api`; remaining stub debt is **SR-10, SR-13, SR-16–SR-17** (SR-08/09/12/15 refreshed Jul 2026).
- [services/data-plane/src/data_plane/main.py](services/data-plane/src/data_plane/main.py) composes ingest + analytics sink — **no additional `stub` hits** in that wrapper from this inventory.

## Follow-ups (Phase 0 exit criteria)

1. Review each **Target disposition** with product (especially **SR-10** BSA validation depth and mock policy **SR-13**).
2. Add CI grep allowlist for legitimate test-only paths after Track verification phase.
3. Re-run this register after each track merges; shrink table rows to zero “Ship” debt.