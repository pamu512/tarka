# Stub Register (Phase 0 inventory)

This register was produced by executing **Phase 0** of [docs/TIER_1_HONESTY_PROGRAM.md](docs/TIER_1_HONESTY_PROGRAM.md): keyword scans across `services/`, `frontend/`, and `packages/`, then manual classification. **No code was changed** as part of this inventory.

## Methodology

- **Keywords searched:** `stub`, `_stub`, `stub_`, `_STORE`, `placeholder` (code/comments, not SQL `{tenant_id:String}` bind tokens alone), module docstrings mentioning `integration stubs`, and related patterns (`echo_stub`, `backtest_run_stub`, `rego_stub`, `metric_stub`).
- **Scopes:** `services/`**, `frontend/`**, `packages/**` (Rust under `services/**` had no `stub`/`TODO`/`FIXME` hits in this pass).
- **Excluded by policy (non-goals for runtime):** `**/tests/`**, `**/test_*.py`, `unittest.mock` / `vi.mock` usage, and comments that only mention “mock” for test harnesses.
- **Disposition values:** **Ship** = replace with durable + real execution; **Delete** = remove HTTP/UI surface until honest; **Degrade** = keep only with explicit, documented contract (e.g. structured 503 / response field `source`), never silent fake metrics.

## Stub Register (runtime and operator-facing)


| ID    | File                                                                                                                                   | Symbol / route / surface                                               | User-visible behavior (today)                                                                                                                                   | Target disposition                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| SR-01 | [services/decision-api/src/decision_api/feature_store_api.py](services/decision-api/src/decision_api/feature_store_api.py)             | `_STORE`, `POST/GET /v1/feature-store/definitions`                     | Definitions stored only in an in-process dict; lost on restart; ClickHouse DDL is generated as text but **not executed**                                        | Ship (Track A)                                                                                     |
| SR-02 | [services/decision-api/src/decision_api/feature_store_api.py](services/decision-api/src/decision_api/feature_store_api.py)             | `_mv_sql` → column alias `metric_stub`                                 | Emitted MV SQL uses a column name implying a stub metric (naming / semantics debt)                                                                              | Ship (rename + real aggregation)                                                                   |
| SR-03 | [services/decision-api/src/decision_api/analytics_dashboards.py](services/decision-api/src/decision_api/analytics_dashboards.py)       | `_stub_kpis`, `GET /v1/analytics/dashboards/kpis`                      | Returns KPI fields as `null` with a `note` explaining CH not wired; may **cache** that payload in Redis — reads like “empty dashboard” rather than hard failure | Ship (Track C)                                                                                     |
| SR-04 | [services/decision-api/src/decision_api/backtest_api.py](services/decision-api/src/decision_api/backtest_api.py)                       | `backtest_run_stub`, `POST /v1/rules/backtest/run`                     | Returns `status: "stub"` and `metrics` with `null` values — explicitly non-production                                                                           | Ship (Track B) or Delete (remove route until engine exists)                                        |
| SR-05 | [services/decision-api/src/decision_api/rule_compiler_api.py](services/decision-api/src/decision_api/rule_compiler_api.py)             | `_compile_to_rego_stub`, `POST /v1/rules/visual/compile` → `rego_stub` | Returns Rego text that is a **non-enforcing** stub (`violations[msg] { false }` pattern) while JSON rule pack may look deployable                               | Ship (Track F)                                                                                     |
| SR-06 | [services/decision-api/src/decision_api/vendors/registry.py](services/decision-api/src/decision_api/vendors/registry.py)               | `_EchoVendor`, `echo_stub`, `list_registered_vendors`                  | Built-in vendor adapter returns synthetic signal with `stub:echo` reason codes; module documents “built-in vendor stubs”                                        | Ship (Track E)                                                                                     |
| SR-07 | [services/decision-api/src/decision_api/vendor_marketplace_api.py](services/decision-api/src/decision_api/vendor_marketplace_api.py)   | `POST /v1/vendors/probe`                                               | Returns `skipped` when unknown vendor / cost gate — honest control flow, depends on stub registry (SR-06) for meaningful signal                                 | Ship (depends on SR-06)                                                                            |
| SR-08 | [services/case-api/src/case_api/sar_filing_transport.py](services/case-api/src/case_api/sar_filing_transport.py)                       | Module + `schedule_ack_poll`                                           | Docstring states **integration stubs**; ACK poll is **log-only** unless `FINCEN_BSA_SFTP_HOST` set; no durable worker or persisted poll state                   | Ship (Track D)                                                                                     |
| SR-09 | [services/case-api/src/case_api/main.py](services/case-api/src/case_api/main.py)                                                       | SAR generate path calling `schedule_ack_poll`                          | User can complete SAR generate flow while ACK machinery is effectively **no-op** in default configs                                                             | Ship (Track D)                                                                                     |
| SR-10 | [services/case-api/src/case_api/sar_filing_transport.py](services/case-api/src/case_api/sar_filing_transport.py)                       | `validate_pre_filing`                                                  | Best-effort substring / tag checks for required “fields”; not a full BSA XML schema validator                                                                   | Ship (validation depth)                                                                            |
| SR-11 | [services/decision-api/src/decision_api/rule_gitops_api.py](services/decision-api/src/decision_api/rule_gitops_api.py)                 | `POST /v1/rules/gitops/approve`                                        | Returns an `audit_token` string only; **no durable** maker/checker record in application database (delegates to “external SOX system” in comment)               | Ship (persist approvals) or Degrade (501 until persisted)                                          |
| SR-12 | [services/decision-api/src/decision_api/reporting_nl.py](services/decision-api/src/decision_api/reporting_nl.py)                       | `POST /v1/reporting/nl-to-sql` when `TARKA_REPORTING_NL_LLM_URL` unset | Returns allowlisted **template** SQL with `source: "template"` and explicit `notes` — degraded but **disclosed** (not silent nulls)                             | Degrade (keep; tighten docs) or Ship (remove template path; require LLM)                           |
| SR-13 | [frontend/src/api/client.ts](frontend/src/api/client.ts)                                                                               | `getMockResponse`, `VITE_USE_API_MOCKS`, `USE_API_MOCKS`               | Optional **mock API fallback** in dev / when forced on; can mask broken backends during demos                                                                   | Degrade (default off in prod — already); Delete (remove mocks) if policy forbids any runtime mocks |
| SR-14 | [frontend/src/pages/VisualRuleBuilder.tsx](frontend/src/pages/VisualRuleBuilder.tsx)                                                   | UI copy                                                                | Surfaces “Rego stub” wording to analysts — reflects SR-05                                                                                                       | Ship (after Track F)                                                                               |
| SR-15 | [services/decision-api/src/decision_api/rule_api.py](services/decision-api/src/decision_api/rule_api.py)                               | Per-rule hit counts (see docstring near metrics)                       | Docstring: hit counts are **in-memory since process start** (not durable cluster-wide)                                                                          | Ship (Redis/DB) or Degrade (document + metrics label)                                              |
| SR-16 | [services/integration-ingress/src/integration_ingress/sanctions.py](services/integration-ingress/src/integration_ingress/sanctions.py) | Docstring: in-memory fuzzy matching                                    | Screening behavior may not meet enterprise persistence/audit expectations if implementation matches docstring                                                   | Ship (clarify + persist)                                                                           |
| SR-17 | [services/investigation-agent/src/investigation_agent/main.py](services/investigation-agent/src/investigation_agent/main.py)           | Tabular upload handler (docstring)                                     | Describes **in-memory TTL** storage for uploaded tabular files — bounded but not durable                                                                        | Ship (disk/redis) or Degrade (strict size + documented volatility)                                 |


## Packages scan (`packages/`)

- **No** `stub` / `_stub` / `_STORE` hits in `packages/tarka-web-sdk` or `packages/fraud-sdk-typescript` source (excluding field names like `mock_location_detected` on types and test `vi.mock`).

## `services/**/*.rs` scan

- **No** matches for `stub`, `TODO`, `FIXME`, or `unimplemented!` in this pass (pattern: whole-word / common markers only).

## Macroservices note

- [services/core-api/src/core_api/main.py](services/core-api/src/core_api/main.py) mounts `decision_api` and `case_api`; **stub debt remains in those sub-apps** (SR-01–SR-12, SR-08–SR-10, SR-11).
- [services/data-plane/src/data_plane/main.py](services/data-plane/src/data_plane/main.py) composes ingest + analytics sink — **no additional `stub` hits** in that wrapper from this inventory.

## Follow-ups (Phase 0 exit criteria)

1. Review each **Target disposition** with product (especially **Delete** vs **Ship** for SR-04 and template path SR-12).
2. Add CI grep allowlist for legitimate test-only paths after Track verification phase.
3. Re-run this register after each track merges; shrink table rows to zero “Ship” debt.